# OEE Platform — Dalga 1 (G6 · G11 · G9) Uygulama Planı

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Ürünü güvenli (G6: CI parite kapısı), değerli (G11: kayıpların TL Pareto'su) ve eyleme dönük (G9: TL'li, önceliklendirilmiş öneriler) hale getirmek.

**Architecture:** Mevcut katmanlı FastAPI + DuckDB mimarisine üç dikey dilim eklenir. G6 yeni davranış eklemez; mevcut doğruluğu açık sabitlerle bir regresyon "sözleşme" testine ve CI kapısına bağlar. G11 kayıp ağacını (`analytics/loss_tree.py`) config tabanlı birim maliyetlerle TL'ye çevirir (`analytics/cost.py`) ve yeni bir endpoint + pano Pareto'su sunar. G9, G11'in TL ağacı üstüne kural tabanlı öneriler kurar; tahmini kazanç modüler bir `GainEstimator` Protokolü arkasındadır (ileride simülatör what-if takılabilsin). **Firewall korunur:** üretim analitiği `ground_truth` ALMAZ; gerçek yalnız doğrulama testlerine girer.

**Tech Stack:** Python 3.11, FastAPI 0.115, DuckDB, pydantic v2, PyYAML, Jinja2 + Chart.js (CDN, build yok), pytest 8, ruff (yeni), GitHub Actions (yeni).

**Çalışma kuralları:**
- Tüm komutlar `oee-platform/backend/` dizininden çalışır (pytest rootdir = `backend`).
- Repo: `Kenobii512/oee-platform`, dal `main`. **Önce feature dalı aç** (aşağıda Task 0).
- Birim maliyetler/öneri oranları **daima config'te**; koda gömülü sayı yok.
- Her görev sonunda `pytest -q` yeşil + `ruff check` temiz kalmalı (G6 sonrası).

---

## Ön koşul — Task 0: Dal + araç kurulumu

**Files:** yok (ortam hazırlığı).

- [ ] **Step 1: Feature dalı aç**

```bash
cd oee-platform
git checkout main && git pull
git checkout -b feat/wave1-g6-g11-g9
```

- [ ] **Step 2: ruff'ı bağımlılığa ekle ve kur**

`backend/requirements.txt` sonuna ekle:

```
ruff==0.8.4
```

Sonra:

```bash
cd backend
pip install -r requirements.txt
```

- [ ] **Step 3: Mevcut yeşil tabanı doğrula**

Run: `cd backend && pytest -q`
Expected: 49 passed (yeni iş öncesi temel).

---

## Dosya yapısı (oluşacak / değişecek)

```
backend/pyproject.toml                         # YENİ — ruff + pytest config (marker: regression)
backend/tests/test_regression_contract.py      # YENİ — G6 eşik sözleşmesi (açık sabitler)
Makefile                                        # YENİ — test / lint / ci hedefleri
.github/workflows/ci.yml                        # YENİ — push/PR'da ruff + pytest (3.11)

config/costs.yaml                               # YENİ — birim maliyetler (sim costs ile hizalı)
backend/app/config.py                           # DEĞİŞ — CostConfig + RecommendConfig yükleme, AppConfig'e yollar
backend/app/analytics/cost.py                   # YENİ — to_tl(loss_tree, costs) -> tl'li liste + total_tl
backend/app/api/cost_routes.py                  # YENİ — GET /loss-tree/cost
backend/tests/test_cost_mapping.py              # YENİ — birim dönüşüm + toplam
backend/tests/test_cost_parity.py               # YENİ — baseline est_cost_tl paritesi
backend/tests/test_cost_endpoint.py             # YENİ — endpoint 200 + sıralı + 6 kategori

config/recommend.yaml                           # YENİ — kategori şablonları + geri-kazanım oranları
backend/app/analytics/recommend.py              # YENİ — GainEstimator Protocol + generate_recommendations
backend/app/api/recommend_routes.py             # YENİ — GET /recommendations
backend/tests/test_recommend_priority.py        # YENİ — en üst DOWNTIME + TL sıralı
backend/tests/test_recommend_content.py         # YENİ — zorunlu alanlar dolu
backend/tests/test_recommend_endpoint.py        # YENİ — endpoint 200 + >=3 öneri

backend/app/main.py                             # DEĞİŞ — cost + recommend router kaydı
backend/app/templates/dashboard.html            # DEĞİŞ — TL Pareto + Öneriler bölümleri
backend/app/static/dashboard.js                 # DEĞİŞ — TL Pareto + Öneriler render
backend/tests/conftest.py                       # DEĞİŞ — baseline_truth_cost yardımcısı
docs/STATUS.md                                  # DEĞİŞ — CI + TL + Öneri notları
```

---

# FAZ G6 — Regresyon / CI kapısı

Amaç: parite eşiklerini açık sabitlerle tek bir sözleşme testine ve CI kapısına bağlamak. Yeni ürün davranışı yok.

## Task G6.1: ruff + pytest config (pyproject.toml)

**Files:**
- Create: `backend/pyproject.toml`

- [ ] **Step 1: pyproject.toml yaz**

```toml
[tool.ruff]
line-length = 100
target-version = "py311"

[tool.ruff.lint]
# Mevcut kodu kırmayacak makul taban: pyflakes (F), pycodestyle (E/W), import sırası (I).
select = ["E", "F", "W", "I"]
ignore = ["E501"]  # satır uzunluğu uyarısını kapıda zorlamıyoruz (mevcut kod uzun satırlar içeriyor)

[tool.pytest.ini_options]
testpaths = ["tests"]
markers = [
    "regression: parite/doğruluk eşiklerini kilitleyen sözleşme testleri (G6 kapısı)",
]
```

- [ ] **Step 2: ruff'ı mevcut kodda çalıştır, otomatik düzeltilebilenleri uygula**

Run: `cd backend && ruff check . --fix`
Expected: import sırası vb. küçük düzeltmeler uygulanır; geriye kalan ihlal **yok**.

- [ ] **Step 3: ruff temiz mi doğrula**

Run: `cd backend && ruff check .`
Expected: `All checks passed!`

- [ ] **Step 4: pytest hâlâ yeşil mi (marker uyarısı kalkmalı)**

Run: `cd backend && pytest -q`
Expected: 49 passed, hiçbir `PytestUnknownMarkWarning` yok.

- [ ] **Step 5: Commit**

```bash
git add backend/pyproject.toml backend/requirements.txt
git add -A   # ruff --fix değişikliklerini de dahil et
git commit -m "chore(g6): add ruff + pytest config (regression marker)"
```

## Task G6.2: Regresyon sözleşme testi

**Files:**
- Create: `backend/tests/test_regression_contract.py`

Bu test parite eşiklerini **açık sabit** olarak tek yerde toplar; mevcut testlerin dağıttığı garantileri kapı için konsolide eder.

- [ ] **Step 1: Testi yaz (önce başarısız olacak — fixture/import yolu doğrulanır)**

```python
"""G6 regresyon sözleşmesi: parite/doğruluk eşiklerini açık sabitlerle kilitler.

Bu modül yeni davranış test etmez; mevcut garantileri (OEE parite, kayıpsız OEE,
gizli kanal geri kazanımı) tek yerde, açık sabitlerle CI kapısına bağlar.
FIREWALL: extract_loss_tree ground_truth ALMAZ; gerçek yalnız doğrulamada (conftest).
"""
import inspect

import pytest

from app.analytics.loss_tree import INFERRED, VISIBLE, extract_loss_tree
from app.analytics.oee import compute_oee
from tests.conftest import FIXTURES, baseline_truth_value, load_fixture_into_repo

# --- Eşikler (sözleşme; değiştirmek = bilinçli karar) ---
PARITY_TOL = 0.01       # OEE baseline paritesi: ±%1
VISIBLE_TOL = 0.01      # Görünür kanallar ground_truth ile ±%1
LOSSLESS_MIN = 0.95     # Kayıpsız sette OEE >= %95
INFERRED_MIN = 0.85     # Gizli kanal (FILL/SPEED) geri kazanımı >= %85

pytestmark = pytest.mark.regression


def test_firewall_extract_loss_tree_has_no_ground_truth():
    sig = inspect.signature(extract_loss_tree)
    assert "ground_truth" not in sig.parameters


def test_visible_channels_within_tolerance(tmp_path, line_def):
    repo = load_fixture_into_repo(FIXTURES / "baseline", str(tmp_path / "b.duckdb"))
    tree = extract_loss_tree(repo.fetch_events(), repo.fetch_production(), line_def)
    for cat in VISIBLE:
        truth = baseline_truth_value(cat)
        assert truth > 0, cat
        assert abs(tree.value(cat) - truth) <= VISIBLE_TOL * truth, (cat, tree.value(cat), truth)
    repo.close()


def test_inferred_channels_recover_min(tmp_path, line_def):
    repo = load_fixture_into_repo(FIXTURES / "baseline", str(tmp_path / "b.duckdb"))
    tree = extract_loss_tree(repo.fetch_events(), repo.fetch_production(), line_def)
    for cat in INFERRED:
        truth = baseline_truth_value(cat)
        assert truth > 0, cat
        assert tree.value(cat) / truth >= INFERRED_MIN, (cat, tree.value(cat), truth)
    repo.close()


def test_lossless_oee_min(tmp_path, line_def):
    repo = load_fixture_into_repo(FIXTURES / "lossless", str(tmp_path / "l.duckdb"))
    oee = compute_oee(repo.fetch_events(), repo.fetch_production(), line_def)
    assert oee.oee >= LOSSLESS_MIN, oee.oee
    repo.close()
```

- [ ] **Step 2: Çalıştır — `compute_oee` imzası/import yolunu doğrula**

Run: `cd backend && pytest tests/test_regression_contract.py -v`
Expected: 4 PASS. Eğer `compute_oee` import'u ya da imzası farklıysa (FAIL/ImportError), `app/analytics/oee.py`'deki gerçek fonksiyon adı ve parametre sırasına göre import ve çağrıyı düzelt (mevcut `test_oee_lossless.py` ve `test_oee_baseline.py` doğru kullanımı gösterir — onları referans al).

- [ ] **Step 3: Yalnız regresyon marker'ı çalışıyor mu**

Run: `cd backend && pytest -m regression -q`
Expected: yalnız bu modüldeki testler seçilir, hepsi PASS.

- [ ] **Step 4: Tüm paket yeşil**

Run: `cd backend && pytest -q && ruff check .`
Expected: 53 passed (49 + 4), ruff temiz.

- [ ] **Step 5: Commit**

```bash
git add backend/tests/test_regression_contract.py
git commit -m "test(g6): regression contract locking parity thresholds"
```

## Task G6.3: Makefile

**Files:**
- Create: `Makefile` (repo kökü)

- [ ] **Step 1: Makefile yaz**

```makefile
.PHONY: test lint ci

test:
	cd backend && pytest -q

lint:
	cd backend && ruff check .

ci: lint test
```

- [ ] **Step 2: Yerelde kapıyı çalıştır**

Run: `make ci`
Expected: ruff temiz, ardından 53 passed. (Windows'ta `make` yoksa: `cd backend && ruff check . && pytest -q` ile aynı kapı doğrulanır.)

- [ ] **Step 3: Commit**

```bash
git add Makefile
git commit -m "chore(g6): add make test/lint/ci targets"
```

## Task G6.4: GitHub Actions CI

**Files:**
- Create: `.github/workflows/ci.yml` (repo kökü)

- [ ] **Step 1: ci.yml yaz**

```yaml
name: CI

on:
  push:
    branches: ["**"]
  pull_request:

jobs:
  test:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: backend
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.11"
      - name: Install deps
        run: pip install -r requirements.txt
      - name: Lint (ruff)
        run: ruff check .
      - name: Test (pytest)
        run: pytest -q
```

- [ ] **Step 2: Commit**

```bash
git add .github/workflows/ci.yml
git commit -m "ci(g6): GitHub Actions ruff + pytest gate (py3.11)"
```

- [ ] **Step 3: Push ve CI'yı doğrula**

```bash
git push -u origin feat/wave1-g6-g11-g9
gh run watch --exit-status
```
Expected: CI **yeşil**. (Doğrulama: testlerden birinin eşiğini geçici bozup push edince CI **kırmızı** olmalı; sonra geri al. Bu adım opsiyonel ama önerilir.)

## Task G6.5: STATUS.md CI notu

**Files:**
- Modify: `docs/STATUS.md`

- [ ] **Step 1: "Çalıştırma" bölümünün hemen üstüne CI notu ekle**

`docs/STATUS.md` içinde `## Çalıştırma` başlığını bul; tam üstüne ekle:

```markdown
## CI (G6)

GitHub Actions (`.github/workflows/ci.yml`) her push/PR'da Python 3.11 üzerinde
`ruff check` + `pytest -q` çalıştırır. Parite eşikleri (OEE ±%1, kayıpsız OEE ≥%95,
gizli kanal geri kazanımı ≥%85, firewall) `backend/tests/test_regression_contract.py`
içinde açık sabitlerle kilitlidir (`@pytest.mark.regression`). Yerelde aynı kapı: `make ci`.

```

- [ ] **Step 2: Commit**

```bash
git add docs/STATUS.md
git commit -m "docs(g6): document CI gate in STATUS"
```

**G6 başarı kriteri:** `make ci` ve GitHub Actions yeşil; herhangi bir eşik ihlali kırmızı; 53 test yeşil.

---

# FAZ G11 — TL (maliyet) lensi

Amaç: kayıp ağacını ortak birime (TL) çevirip kategoriler-arası tek Pareto + parasal toplam sunmak.

**Maliyet modeli (sim `costs` ile hizalı):** DOWNTIME/MICROSTOP = dk × 50, SPEED_LOSS = dk × 20, FILL = parça × 2, REDO = parça × 3, SCRAP = parça × 8.

## Task G11.1: costs.yaml + CostConfig yükleme

**Files:**
- Create: `config/costs.yaml` (repo kökü)
- Modify: `backend/app/config.py`

- [ ] **Step 1: costs.yaml yaz (değerler sim scenario_baseline.yaml costs bloğundan)**

```yaml
# Birim maliyetler — simülatör scenario_baseline.yaml `costs` bloğuyla hizalı (parite için).
# Mikro duruş, simülatörde duruş oranıyla maliyetlenir (line.py) -> downtime ile aynı oran.
downtime_tl_per_min: 50.0
microstop_tl_per_min: 50.0
speed_tl_per_min: 20.0
fill_tl_per_part: 2.0
redo_tl_per_part: 3.0
scrap_tl_per_part: 8.0
```

- [ ] **Step 2: config.py'ye CostConfig + yükleyici + AppConfig yolu ekle**

`backend/app/config.py` içinde `AppConfig` dataclass'ına alan ekle:

```python
@dataclass(frozen=True)
class AppConfig:
    duckdb_path: str
    line_config_path: str
    cost_config_path: str
```

`load_app_config` içine cost yolu ekle (mevcut `line_config_path` satırının hemen ardına, aynı `_CONFIG_DIR` mantığıyla):

```python
def load_app_config() -> AppConfig:
    """Env'den uygulama ayarlarını okur (varsayılanlarla)."""
    config_dir = Path(__file__).resolve().parents[2] / "config"
    return AppConfig(
        duckdb_path=os.environ.get("OEE_DUCKDB_PATH", "oee.duckdb"),
        line_config_path=os.environ.get(
            "OEE_LINE_CONFIG", str(config_dir / "line_default.yaml")
        ),
        cost_config_path=os.environ.get(
            "OEE_COST_CONFIG", str(config_dir / "costs.yaml")
        ),
    )
```

Dosya sonuna `CostConfig` + yükleyici ekle:

```python
@dataclass(frozen=True)
class CostConfig:
    """Kategori bazında birim maliyetler (TL). Kodda gömülü sayı yok — daima config'ten."""

    downtime_tl_per_min: float
    microstop_tl_per_min: float
    speed_tl_per_min: float
    fill_tl_per_part: float
    redo_tl_per_part: float
    scrap_tl_per_part: float


def load_cost_config(path: str | Path) -> CostConfig:
    with open(path, encoding="utf-8") as f:
        raw = yaml.safe_load(f)
    return CostConfig(
        downtime_tl_per_min=float(raw["downtime_tl_per_min"]),
        microstop_tl_per_min=float(raw["microstop_tl_per_min"]),
        speed_tl_per_min=float(raw["speed_tl_per_min"]),
        fill_tl_per_part=float(raw["fill_tl_per_part"]),
        redo_tl_per_part=float(raw["redo_tl_per_part"]),
        scrap_tl_per_part=float(raw["scrap_tl_per_part"]),
    )
```

- [ ] **Step 3: Yükleyici çalışıyor mu (hızlı doğrulama)**

Run:
```bash
cd backend && python -c "from app.config import load_app_config, load_cost_config; c=load_app_config(); print(load_cost_config(c.cost_config_path))"
```
Expected: `CostConfig(downtime_tl_per_min=50.0, ... scrap_tl_per_part=8.0)`

- [ ] **Step 4: Commit**

```bash
git add config/costs.yaml backend/app/config.py
git commit -m "feat(g11): cost config (costs.yaml + CostConfig loader)"
```

## Task G11.2: cost.py — TL dönüşümü + test_cost_mapping

**Files:**
- Create: `backend/app/analytics/cost.py`
- Test: `backend/tests/test_cost_mapping.py`

- [ ] **Step 1: Başarısız testi yaz**

```python
from app.analytics.cost import to_tl
from app.analytics.loss_tree import LossEntry, LossTree
from app.config import CostConfig

COSTS = CostConfig(
    downtime_tl_per_min=50.0, microstop_tl_per_min=50.0, speed_tl_per_min=20.0,
    fill_tl_per_part=2.0, redo_tl_per_part=3.0, scrap_tl_per_part=8.0,
)


def _tree():
    return LossTree({
        "DOWNTIME": LossEntry(minutes=10.0),
        "MICROSTOP": LossEntry(minutes=4.0),
        "SPEED_LOSS": LossEntry(minutes=5.0),
        "FILL_LOSS": LossEntry(parts=20.0),
        "QUALITY_REDO": LossEntry(parts=6.0),
        "QUALITY_SCRAP": LossEntry(parts=3.0),
    })


def test_unit_conversions():
    res = to_tl(_tree(), COSTS)
    tl = {c["category"]: c["tl"] for c in res["categories"]}
    assert tl["DOWNTIME"] == 10.0 * 50.0      # 500
    assert tl["MICROSTOP"] == 4.0 * 50.0      # 200
    assert tl["SPEED_LOSS"] == 5.0 * 20.0     # 100
    assert tl["FILL_LOSS"] == 20.0 * 2.0      # 40
    assert tl["QUALITY_REDO"] == 6.0 * 3.0    # 18
    assert tl["QUALITY_SCRAP"] == 3.0 * 8.0   # 24


def test_total_is_sum_of_categories():
    res = to_tl(_tree(), COSTS)
    assert res["total_tl"] == sum(c["tl"] for c in res["categories"])
    assert res["total_tl"] == 500 + 200 + 100 + 40 + 18 + 24


def test_sorted_descending_by_tl():
    res = to_tl(_tree(), COSTS)
    tls = [c["tl"] for c in res["categories"]]
    assert tls == sorted(tls, reverse=True)
    assert res["categories"][0]["category"] == "DOWNTIME"


def test_entries_carry_axis_value_kind():
    res = to_tl(_tree(), COSTS)
    by = {c["category"]: c for c in res["categories"]}
    assert by["DOWNTIME"]["axis"] == "minutes" and by["DOWNTIME"]["kind"] == "visible"
    assert by["FILL_LOSS"]["axis"] == "parts" and by["FILL_LOSS"]["kind"] == "inferred"
    assert by["DOWNTIME"]["value"] == 10.0
```

- [ ] **Step 2: Çalıştır — başarısız olmalı**

Run: `cd backend && pytest tests/test_cost_mapping.py -q`
Expected: FAIL (`ModuleNotFoundError: app.analytics.cost`).

- [ ] **Step 3: cost.py implementasyonu**

```python
"""Kayıp ağacını ortak birime (TL) çevirir → kategoriler-arası tek Pareto + parasal toplam.

Birim maliyetler CostConfig'ten gelir (config/costs.yaml); kodda gömülü sayı YOK.
Maliyet modeli simülatör `costs` bloğuyla hizalıdır (parite için):
  DOWNTIME/MICROSTOP = dk × downtime/microstop oranı, SPEED_LOSS = dk × speed oranı,
  FILL/REDO/SCRAP = parça × ilgili parça oranı.
FIREWALL: yalnız genel kayıp ağacı + config maliyetleri kullanılır; ground_truth ALMAZ.
"""
from __future__ import annotations

from app.analytics.loss_tree import CATEGORIES, LossTree, axis_of, kind_of
from app.config import CostConfig

# Kategori -> CostConfig alanı. Tek doğruluk kaynağı (DRY).
_RATE_FIELD = {
    "DOWNTIME": "downtime_tl_per_min",
    "MICROSTOP": "microstop_tl_per_min",
    "SPEED_LOSS": "speed_tl_per_min",
    "FILL_LOSS": "fill_tl_per_part",
    "QUALITY_REDO": "redo_tl_per_part",
    "QUALITY_SCRAP": "scrap_tl_per_part",
}


def category_tl(category: str, tree: LossTree, costs: CostConfig) -> float:
    """Tek kategorinin TL karşılığı (doğal eksen × ilgili birim oran)."""
    rate = getattr(costs, _RATE_FIELD[category])
    return tree.value(category) * rate


def to_tl(tree: LossTree, costs: CostConfig) -> dict:
    """Kayıp ağacını TL'ye çevirir; TL'ye göre AZALAN sıralı liste + total_tl döner.

    Dönen yapı:
      {"categories": [{category, axis, value, tl, kind}, ...], "total_tl": float}
    """
    cats = [
        {
            "category": c,
            "axis": axis_of(c),
            "value": tree.value(c),
            "tl": category_tl(c, tree, costs),
            "kind": kind_of(c),
        }
        for c in CATEGORIES
    ]
    cats.sort(key=lambda x: x["tl"], reverse=True)
    return {"categories": cats, "total_tl": sum(c["tl"] for c in cats)}
```

- [ ] **Step 4: Çalıştır — geçmeli**

Run: `cd backend && pytest tests/test_cost_mapping.py -q`
Expected: 4 passed.

- [ ] **Step 5: Commit**

```bash
git add backend/app/analytics/cost.py backend/tests/test_cost_mapping.py
git commit -m "feat(g11): cost.to_tl mapping + unit tests"
```

## Task G11.3: Baseline TL paritesi

**Files:**
- Modify: `backend/tests/conftest.py`
- Test: `backend/tests/test_cost_parity.py`

Parite nüansı: simülatör REDO maliyetine strip downtime'ını, SCRAP'a carrier `loaded_qty`'sini ekler → zaman kanalları **sıkı** (±%2), kalite kanalları **geniş bant** (±%30), toplam makul bant.

- [ ] **Step 1: conftest'e ground_truth TL yardımcısı ekle**

`backend/tests/conftest.py` içine, `baseline_truth_value`'nun hemen altına ekle:

```python
def baseline_truth_cost(category: str) -> float:
    """ground_truth.csv `est_cost_tl` toplamı (kategori bazında). Yalnız doğrulamada."""
    total = 0.0
    with open(FIXTURES / "baseline" / "ground_truth.csv", encoding="utf-8") as f:
        for r in csv.DictReader(f):
            if r["event_type"] == category:
                total += float(r["est_cost_tl"])
    return total
```

- [ ] **Step 2: Parite testini yaz**

```python
"""G11 baseline TL paritesi (firewall: gerçek yalnız doğrulamada).

Zaman kanalları (DOWNTIME/MICROSTOP/SPEED) ground_truth est_cost_tl ile sıkı (±%2);
kalite kanalları (REDO/SCRAP) sim'in ek bookleme'leri nedeniyle geniş bant (±%30);
toplam makul bant içinde.
"""
from app.analytics.cost import to_tl
from app.analytics.loss_tree import extract_loss_tree
from app.config import load_cost_config
from tests.conftest import (
    FIXTURES,
    baseline_truth_cost,
    load_fixture_into_repo,
)

COSTS_PATH = FIXTURES.parents[2] / "config" / "costs.yaml"

TIME_TOL = 0.02      # zaman kanalları ±%2
QUALITY_TOL = 0.30   # kalite kanalları ±%30
TOTAL_TOL = 0.15     # toplam makul bant ±%15


def _platform_tl(tmp_path, line_def):
    repo = load_fixture_into_repo(FIXTURES / "baseline", str(tmp_path / "b.duckdb"))
    tree = extract_loss_tree(repo.fetch_events(), repo.fetch_production(), line_def)
    costs = load_cost_config(COSTS_PATH)
    res = to_tl(tree, costs)
    repo.close()
    return {c["category"]: c["tl"] for c in res["categories"]}, res["total_tl"]


def test_time_channels_tight_parity(tmp_path, line_def):
    tl, _ = _platform_tl(tmp_path, line_def)
    for cat in ("DOWNTIME", "MICROSTOP", "SPEED_LOSS"):
        truth = baseline_truth_cost(cat)
        assert truth > 0, cat
        assert abs(tl[cat] - truth) <= TIME_TOL * truth, (cat, tl[cat], truth)


def test_quality_channels_wide_band(tmp_path, line_def):
    tl, _ = _platform_tl(tmp_path, line_def)
    for cat in ("QUALITY_REDO", "QUALITY_SCRAP"):
        truth = baseline_truth_cost(cat)
        assert truth > 0, cat
        assert abs(tl[cat] - truth) <= QUALITY_TOL * truth, (cat, tl[cat], truth)


def test_total_within_reasonable_band(tmp_path, line_def):
    tl, total = _platform_tl(tmp_path, line_def)
    truth_total = sum(
        baseline_truth_cost(c)
        for c in ("DOWNTIME", "MICROSTOP", "SPEED_LOSS",
                  "FILL_LOSS", "QUALITY_REDO", "QUALITY_SCRAP")
    )
    assert abs(total - truth_total) <= TOTAL_TOL * truth_total, (total, truth_total)


def test_downtime_is_largest_tl_loss(tmp_path, line_def):
    tl, _ = _platform_tl(tmp_path, line_def)
    assert tl["DOWNTIME"] == max(tl.values())
```

- [ ] **Step 3: Çalıştır ve bantları kalibre et**

Run: `cd backend && pytest tests/test_cost_parity.py -v`
Expected: 4 passed. Eğer kalite kanalı ya da toplam beklenenden saparsa, **eşik sabitlerini gerçek sapmaya göre genişlet** (briefte kalite "geniş bant", toplam "makul bant" olarak bilinçli gevşek). Sapmayı assert mesajından oku; `QUALITY_TOL`/`TOTAL_TOL`'u gözlenen değerin hemen üstüne yuvarla ve yorumda gerçek oranı not et. Zaman kanalları ±%2'yi geçerse bu bir **hata** sinyalidir — cost modelini/oranları gözden geçir.

- [ ] **Step 4: Commit**

```bash
git add backend/tests/conftest.py backend/tests/test_cost_parity.py
git commit -m "test(g11): baseline TL parity (tight time / wide quality)"
```

## Task G11.4: GET /loss-tree/cost endpoint

**Files:**
- Create: `backend/app/api/cost_routes.py`
- Modify: `backend/app/main.py`
- Test: `backend/tests/test_cost_endpoint.py`

- [ ] **Step 1: Endpoint testini yaz**

```python
from pathlib import Path

from fastapi.testclient import TestClient

from app.main import app

FIX = Path(__file__).resolve().parent / "fixtures" / "baseline"


def test_cost_endpoint(tmp_path, monkeypatch):
    monkeypatch.setenv("OEE_DUCKDB_PATH", str(tmp_path / "cost.duckdb"))
    with TestClient(app) as client:
        client.post("/ingest", json={"path": str(FIX)})
        r = client.get("/loss-tree/cost")
        assert r.status_code == 200
        body = r.json()
        cats = body["categories"]
        assert len(cats) == 6
        assert {c["category"] for c in cats} == {
            "DOWNTIME", "MICROSTOP", "QUALITY_REDO",
            "QUALITY_SCRAP", "FILL_LOSS", "SPEED_LOSS",
        }
        for c in cats:
            assert set(c) >= {"category", "axis", "value", "tl", "kind"}
        tls = [c["tl"] for c in cats]
        assert tls == sorted(tls, reverse=True)          # TL azalan
        assert cats[0]["category"] == "DOWNTIME"          # en pahalı kayıp
        assert body["total_tl"] == sum(tls)
```

- [ ] **Step 2: Çalıştır — başarısız olmalı (404)**

Run: `cd backend && pytest tests/test_cost_endpoint.py -q`
Expected: FAIL (route yok → 404).

- [ ] **Step 3: cost_routes.py yaz**

```python
"""GET /loss-tree/cost?from=&to= -> kayıp ağacının TL Pareto'su (azalan) + total_tl.

Native /loss-tree ile aynı dönem mantığı (G4 MVP: from/to yalnız events'e). Birim
maliyetler config'ten (CostConfig); ground_truth ALMAZ (firewall).
"""
from __future__ import annotations

from fastapi import APIRouter, Query, Request

from app.analytics.cost import to_tl
from app.analytics.loss_tree import extract_loss_tree
from app.config import load_cost_config, load_line_definition

router = APIRouter()


@router.get("/loss-tree/cost")
def get_loss_tree_cost(
    request: Request,
    frm: str | None = Query(None, alias="from"),
    to: str | None = Query(None),
) -> dict:
    repo = request.app.state.repo
    cfg = request.app.state.config
    line = load_line_definition(cfg.line_config_path)
    costs = load_cost_config(cfg.cost_config_path)
    events = repo.fetch_events(frm, to)
    production = repo.fetch_production()
    tree = extract_loss_tree(events, production, line)
    return to_tl(tree, costs)
```

- [ ] **Step 4: main.py'de router'ı kaydet**

`backend/app/main.py` — import bloğuna (loss_tree_router satırının ardına):

```python
from app.api.cost_routes import router as cost_router
```

`include_router` bloğuna (`app.include_router(loss_tree_router)` satırının hemen altına):

```python
app.include_router(cost_router)
```

- [ ] **Step 5: Çalıştır — geçmeli**

Run: `cd backend && pytest tests/test_cost_endpoint.py -q`
Expected: passed.

- [ ] **Step 6: Tüm paket + lint**

Run: `cd backend && pytest -q && ruff check .`
Expected: tüm testler yeşil, ruff temiz.

- [ ] **Step 7: Commit**

```bash
git add backend/app/api/cost_routes.py backend/app/main.py backend/tests/test_cost_endpoint.py
git commit -m "feat(g11): GET /loss-tree/cost endpoint"
```

## Task G11.5: Pano — TL Pareto'su

**Files:**
- Modify: `backend/app/templates/dashboard.html`
- Modify: `backend/app/static/dashboard.js`

- [ ] **Step 1: dashboard.html'e TL Pareto kartı ekle**

Önce yerleşim için kayıp ağacı bölümünü bul:

Run: `cd backend && grep -n 'loss-time\|loss-parts\|<canvas' app/templates/dashboard.html`

Kayıp ağacı canvas'larını içeren `<section>` / kartın **hemen ardına** şu bloğu ekle (mevcut kart/section markup desenini birebir taklit et — sınıf adları dosyadaki kardeş kartlarla aynı olsun):

```html
<section class="card" id="cost-pareto-card">
  <header class="card-head">
    <h3>Maliyet Pareto'su (TL)</h3>
    <span class="muted">varsayım: birim maliyetler config/costs.yaml</span>
  </header>
  <div class="kpi-line"><span class="muted">Toplam kayıp:</span>
    <strong id="cost-total">—</strong></div>
  <canvas id="cost-pareto" height="220"></canvas>
</section>
```

> Not: `card` / `card-head` / `muted` sınıfları dosyada mevcut değilse, **kardeş kartların gerçek sınıf adlarını** kullan (grep çıktısındaki komşu markup'ı kopiala). Amaç görsel tutarlılık.

- [ ] **Step 2: dashboard.js'e TL Pareto render ekle**

`renderLossTree` fonksiyonunun hemen ardına ekle:

```javascript
function renderCostPareto(cost) {
  const cats = cost.categories;  // zaten TL azalan sıralı (backend)
  document.getElementById("cost-total").textContent =
    Math.round(cost.total_tl).toLocaleString("tr-TR") + " TL";
  destroy("cost-pareto");
  charts["cost-pareto"] = new Chart(document.getElementById("cost-pareto"), {
    type: "bar",
    data: {
      labels: cats.map((c) => c.category + (c.kind === "inferred" ? " · çıkarım" : "")),
      datasets: [{
        data: cats.map((c) => Math.round(c.tl)),
        backgroundColor: cats.map((c) => c.kind === "inferred" ? C.inferred : C.loss),
        ...bar,
      }],
    },
    options: {
      indexAxis: "y",
      plugins: {
        legend: { display: false },
        tooltip: { callbacks: { label: (ctx) =>
          ctx.raw.toLocaleString("tr-TR") + " TL" } },
      },
      scales: {
        x: { grid, beginAtZero: true, ticks: { callback: (v) => v.toLocaleString("tr-TR") } },
        y: { grid: { display: false } },
      },
    },
  });
}
```

`load()` içindeki `Promise.all` çağrısına `/loss-tree/cost` ekle ve render et. Mevcut:

```javascript
  const [tree, trend, dq] = await Promise.all([
    getJSON("/loss-tree" + q),
    getJSON("/oee/trend?bucket=day" + (q ? "&" + q.slice(1) : "")),
    getJSON("/data-quality/summary"),
  ]);
  renderKpis(oee, dq);
  renderWaterfall(oee);
  renderLossTree(tree);
  renderTrend(trend);
```

Şu hale getir:

```javascript
  const [tree, cost, trend, dq] = await Promise.all([
    getJSON("/loss-tree" + q),
    getJSON("/loss-tree/cost" + q),
    getJSON("/oee/trend?bucket=day" + (q ? "&" + q.slice(1) : "")),
    getJSON("/data-quality/summary"),
  ]);
  renderKpis(oee, dq);
  renderWaterfall(oee);
  renderLossTree(tree);
  renderCostPareto(cost);
  renderTrend(trend);
```

- [ ] **Step 3: Panoyu gözle doğrula**

Run (arka planda):
```bash
cd backend && OEE_DUCKDB_PATH=./demo_g11.duckdb SAMPLE_DATA_DIR=./tests/fixtures/baseline uvicorn app.main:app --port 8011
```
`http://localhost:8011/` aç → "Maliyet Pareto'su (TL)" kartı görünür; en üst çubuk **DOWNTIME** (~11,5k TL), FILL parça olarak büyük ama TL küçük. Sunucuyu durdur.

- [ ] **Step 4: Commit**

```bash
git add backend/app/templates/dashboard.html backend/app/static/dashboard.js
git commit -m "feat(g11): dashboard TL Pareto section"
```

**G11 başarı kriteri:** `/loss-tree/cost` 6 kategoriyi TL azalan döndürür; baseline'da en büyük TL kaybı = DOWNTIME; zaman kanalları ±%2 parite; tüm testler + G6 kapısı yeşil.

---

# FAZ G9 — Kural tabanlı öneri motoru

Amaç: TL'li kayıp ağacından önceliklendirilmiş, somut, şeffaf öneriler. Tahmini kazanç modüler `GainEstimator` arkasında. Abartılı kesinlik/garanti dili YOK.

## Task G9.1: recommend.yaml + yükleme

**Files:**
- Create: `config/recommend.yaml` (repo kökü)
- Modify: `backend/app/config.py`

- [ ] **Step 1: recommend.yaml yaz**

```yaml
# Kategori başına öneri şablonu + geri-kazanım varsayım oranı (config'te; koda gömülü yok).
# {detail} alanı DOWNTIME/MICROSTOP'ta events'ten türetilen dağılımla doldurulur.
defaults:
  recovery_ratio: 0.20   # genel varsayılan geri-kazanım oranı
categories:
  DOWNTIME:
    recovery_ratio: 0.30
    title: "Duruşları azalt"
    action: "En sık duruş nedeni {detail}. MTBF artır: yedek-parça hazırlığı + önleyici bakım planı."
    assumption: "Duruş TL'sinin ~%{pct} kadarı önleyici bakımla geri kazanılabilir varsayımı."
  MICROSTOP:
    recovery_ratio: 0.20
    title: "Mikro duruşları düşür"
    action: "Mikro duruşlar {detail} yoğun; çevrim/akış iyileştir. Operatör neden-giriş kapsamı düşük olabilir (/data-quality) — kök-neden için veri kalitesi de ele alınmalı."
    assumption: "Mikro duruş TL'sinin ~%{pct} kadarı akış iyileştirmesiyle geri kazanılabilir varsayımı."
  SPEED_LOSS:
    recovery_ratio: 0.15
    title: "Hız kaybını azalt"
    action: "Tank/banyo sürelerinde sapma; darboğaz banyo süresini standartlaştır."
    assumption: "Hız kaybı TL'sinin ~%{pct} kadarı süre standardizasyonuyla geri kazanılabilir varsayımı."
  FILL_LOSS:
    recovery_ratio: 0.20
    title: "Askı doluluğunu artır"
    action: "Askı doluluğunu artır (parça olarak büyük kayıp; TL etkisi düşük olsa da kapasite kazancı)."
    assumption: "Eksik doluluğun ~%{pct} kadarı yükleme disiplini ile geri kazanılabilir varsayımı."
  QUALITY_REDO:
    recovery_ratio: 0.25
    title: "Yeniden işlemeyi azalt"
    action: "Kalınlık spec sapması; amper-saat/banyo kontrolü. Redo azalınca strip maliyeti de düşer."
    assumption: "Redo TL'sinin ~%{pct} kadarı proses kontrolüyle geri kazanılabilir varsayımı."
  QUALITY_SCRAP:
    recovery_ratio: 0.25
    title: "Hurdayı azalt"
    action: "Kalınlık/yüzey spec sapması; banyo kontrolü ve giriş kalite kontrolü."
    assumption: "Hurda TL'sinin ~%{pct} kadarı proses kontrolüyle geri kazanılabilir varsayımı."
```

- [ ] **Step 2: config.py'ye RecommendConfig + yükleyici + AppConfig yolu ekle**

`AppConfig`'e alan ekle:

```python
@dataclass(frozen=True)
class AppConfig:
    duckdb_path: str
    line_config_path: str
    cost_config_path: str
    recommend_config_path: str
```

`load_app_config` dönüşüne ekle (cost_config_path satırının ardına):

```python
        recommend_config_path=os.environ.get(
            "OEE_RECOMMEND_CONFIG", str(config_dir / "recommend.yaml")
        ),
```

Dosya sonuna ekle:

```python
@dataclass(frozen=True)
class CategoryRule:
    recovery_ratio: float
    title: str
    action: str
    assumption: str


@dataclass(frozen=True)
class RecommendConfig:
    default_recovery_ratio: float
    rules: dict[str, CategoryRule]


def load_recommend_config(path: str | Path) -> RecommendConfig:
    with open(path, encoding="utf-8") as f:
        raw = yaml.safe_load(f)
    default_ratio = float(raw.get("defaults", {}).get("recovery_ratio", 0.2))
    rules = {
        cat: CategoryRule(
            recovery_ratio=float(r.get("recovery_ratio", default_ratio)),
            title=r["title"],
            action=r["action"],
            assumption=r["assumption"],
        )
        for cat, r in raw.get("categories", {}).items()
    }
    return RecommendConfig(default_recovery_ratio=default_ratio, rules=rules)
```

- [ ] **Step 3: Yükleyici doğrula**

Run:
```bash
cd backend && python -c "from app.config import load_app_config, load_recommend_config; c=load_app_config(); rc=load_recommend_config(c.recommend_config_path); print(list(rc.rules), rc.rules['DOWNTIME'].recovery_ratio)"
```
Expected: 6 kategori listelenir, `DOWNTIME` oranı `0.3`.

- [ ] **Step 4: Commit**

```bash
git add config/recommend.yaml backend/app/config.py
git commit -m "feat(g9): recommend config (templates + recovery ratios)"
```

## Task G9.2: recommend.py — GainEstimator + üretim + içerik testi

**Files:**
- Create: `backend/app/analytics/recommend.py`
- Test: `backend/tests/test_recommend_content.py`

- [ ] **Step 1: İçerik testini yaz**

```python
from app.analytics.recommend import RatioGainEstimator, generate_recommendations
from app.config import CategoryRule, RecommendConfig

RC = RecommendConfig(
    default_recovery_ratio=0.2,
    rules={
        "DOWNTIME": CategoryRule(0.30, "Duruşları azalt", "neden {detail}", "~%{pct} geri kazanım"),
        "MICROSTOP": CategoryRule(0.20, "Mikro", "yoğun {detail}", "~%{pct}"),
        "SPEED_LOSS": CategoryRule(0.15, "Hız", "standardize et", "~%{pct}"),
        "FILL_LOSS": CategoryRule(0.20, "Doluluk", "artır", "~%{pct}"),
        "QUALITY_REDO": CategoryRule(0.25, "Redo", "kontrol", "~%{pct}"),
        "QUALITY_SCRAP": CategoryRule(0.25, "Scrap", "kontrol", "~%{pct}"),
    },
)

COST = {
    "categories": [
        {"category": "DOWNTIME", "axis": "minutes", "value": 230.0, "tl": 11500.0, "kind": "visible"},
        {"category": "MICROSTOP", "axis": "minutes", "value": 60.0, "tl": 3000.0, "kind": "visible"},
        {"category": "SPEED_LOSS", "axis": "minutes", "value": 100.0, "tl": 2000.0, "kind": "inferred"},
        {"category": "FILL_LOSS", "axis": "parts", "value": 700.0, "tl": 1400.0, "kind": "inferred"},
        {"category": "QUALITY_SCRAP", "axis": "parts", "value": 50.0, "tl": 400.0, "kind": "visible"},
        {"category": "QUALITY_REDO", "axis": "parts", "value": 60.0, "tl": 180.0, "kind": "visible"},
    ],
    "total_tl": 18480.0,
}

EVENTS = [
    {"event_type": "DOWNTIME", "duration": 100.0, "reason_code": "ariza_pompa", "station_id": "T3"},
    {"event_type": "DOWNTIME", "duration": 60.0, "reason_code": "ariza_pompa", "station_id": "T3"},
    {"event_type": "DOWNTIME", "duration": 70.0, "reason_code": "kalip_degisim", "station_id": "T1"},
    {"event_type": "MICROSTOP", "duration": 30.0, "reason_code": None, "station_id": "T2"},
    {"event_type": "MICROSTOP", "duration": 30.0, "reason_code": None, "station_id": "T2"},
]


def test_every_recommendation_has_required_fields():
    recs = generate_recommendations(COST, EVENTS, RC, RatioGainEstimator(RC))
    assert recs, "öneri üretilmedi"
    for r in recs:
        assert r["category"]
        assert r["tl"] > 0
        assert r["estimated_gain_tl"] >= 0
        assert r["title"]
        assert r["action"]
        assert r["assumption"]


def test_gain_is_tl_times_recovery_ratio():
    recs = generate_recommendations(COST, EVENTS, RC, RatioGainEstimator(RC))
    by = {r["category"]: r for r in recs}
    assert abs(by["DOWNTIME"]["estimated_gain_tl"] - 11500.0 * 0.30) < 1e-6
    assert abs(by["SPEED_LOSS"]["estimated_gain_tl"] - 2000.0 * 0.15) < 1e-6


def test_downtime_detail_embeds_top_reason():
    recs = generate_recommendations(COST, EVENTS, RC, RatioGainEstimator(RC))
    by = {r["category"]: r for r in recs}
    # En çok dakikayı tüketen neden: ariza_pompa (160 dk).
    assert "ariza_pompa" in by["DOWNTIME"]["action"]


def test_no_guarantee_language():
    recs = generate_recommendations(COST, EVENTS, RC, RatioGainEstimator(RC))
    banned = ("garanti", "kesin", "kesinlikle")
    for r in recs:
        text = (r["action"] + " " + r["assumption"]).lower()
        assert not any(b in text for b in banned), r
```

- [ ] **Step 2: Çalıştır — başarısız olmalı**

Run: `cd backend && pytest tests/test_recommend_content.py -q`
Expected: FAIL (`ModuleNotFoundError: app.analytics.recommend`).

- [ ] **Step 3: recommend.py implementasyonu**

```python
"""Kural tabanlı öneri motoru (G9) — TL'li kayıp ağacı üstüne.

Tasarım: MVP kural tabanlı. Tahmini kazanç MODÜLER bir GainEstimator Protokolü
arkasındadır; varsayılan oran-tabanlıdır (TL × config geri-kazanım oranı). İleride
simülatör destekli what-if aynı arayüze takılır. Abartılı kesinlik/garanti dili YOK;
her öneride varsayım açıkça yazılır.
"""
from __future__ import annotations

from collections import defaultdict
from typing import Protocol

from app.config import RecommendConfig


class GainEstimator(Protocol):
    """Bir kategori için tahmini TL kazancı. Modüler: sim what-if ileride buraya takılır."""

    def estimate(self, category: str, tl: float) -> float: ...


class RatioGainEstimator:
    """Varsayılan tahminci: kazanç = TL × kategori geri-kazanım oranı (config'ten)."""

    def __init__(self, config: RecommendConfig) -> None:
        self._config = config

    def _ratio(self, category: str) -> float:
        rule = self._config.rules.get(category)
        return rule.recovery_ratio if rule else self._config.default_recovery_ratio

    def estimate(self, category: str, tl: float) -> float:
        return tl * self._ratio(category)


def _top_reason_detail(events: list[dict], category: str) -> str:
    """DOWNTIME/MICROSTOP için en çok süreyi tüketen neden/istasyonu metne çevirir."""
    by_reason: dict[str, float] = defaultdict(float)
    by_station: dict[str, float] = defaultdict(float)
    for e in events:
        if e.get("event_type") != category:
            continue
        dur = float(e.get("duration") or 0.0)
        reason = e.get("reason_code")
        if reason:
            by_reason[reason] += dur
        station = e.get("station_id")
        if station:
            by_station[station] += dur
    if by_reason:
        top = max(by_reason, key=by_reason.get)
        return f"`{top}` (en yüksek süre payı)"
    if by_station:
        top = max(by_station, key=by_station.get)
        return f"`{top}` istasyonunda yoğun"
    return "neden kaydı yetersiz (operatör giriş kapsamı düşük)"


def generate_recommendations(
    cost_tree: dict,
    events: list[dict],
    config: RecommendConfig,
    estimator: GainEstimator,
) -> list[dict]:
    """cost_tree'yi (TL azalan) alır, her kategori için config şablonunu doldurur.

    Dönen her öğe: {category, tl, estimated_gain_tl, recovery_ratio, title, action,
    assumption, axis, value, kind}. Liste TL azalan (cost_tree sırasını korur).
    """
    recs: list[dict] = []
    for entry in cost_tree["categories"]:
        cat = entry["category"]
        rule = config.rules.get(cat)
        if rule is None or entry["tl"] <= 0:
            continue
        ratio = rule.recovery_ratio
        detail = (
            _top_reason_detail(events, cat)
            if cat in ("DOWNTIME", "MICROSTOP")
            else ""
        )
        pct = f"{round(ratio * 100)}"
        recs.append(
            {
                "category": cat,
                "axis": entry["axis"],
                "value": entry["value"],
                "kind": entry["kind"],
                "tl": entry["tl"],
                "estimated_gain_tl": estimator.estimate(cat, entry["tl"]),
                "recovery_ratio": ratio,
                "title": rule.title,
                "action": rule.action.format(detail=detail, pct=pct),
                "assumption": rule.assumption.format(detail=detail, pct=pct),
            }
        )
    return recs
```

- [ ] **Step 4: Çalıştır — geçmeli**

Run: `cd backend && pytest tests/test_recommend_content.py -q`
Expected: 4 passed.

- [ ] **Step 5: Commit**

```bash
git add backend/app/analytics/recommend.py backend/tests/test_recommend_content.py
git commit -m "feat(g9): rule-based recommend engine + GainEstimator protocol"
```

## Task G9.3: Baseline öncelik testi

**Files:**
- Test: `backend/tests/test_recommend_priority.py`

- [ ] **Step 1: Önceliklendirme testini yaz**

```python
"""G9 baseline: en üst öneri DOWNTIME hedefli, liste TL azalan."""
from app.analytics.cost import to_tl
from app.analytics.loss_tree import extract_loss_tree
from app.analytics.recommend import RatioGainEstimator, generate_recommendations
from app.config import load_cost_config, load_recommend_config
from tests.conftest import FIXTURES, load_fixture_into_repo

COSTS_PATH = FIXTURES.parents[2] / "config" / "costs.yaml"
RECO_PATH = FIXTURES.parents[2] / "config" / "recommend.yaml"


def _recs(tmp_path, line_def):
    repo = load_fixture_into_repo(FIXTURES / "baseline", str(tmp_path / "b.duckdb"))
    events = repo.fetch_events()
    tree = extract_loss_tree(events, repo.fetch_production(), line_def)
    cost = to_tl(tree, load_cost_config(COSTS_PATH))
    rc = load_recommend_config(RECO_PATH)
    recs = generate_recommendations(cost, events, rc, RatioGainEstimator(rc))
    repo.close()
    return recs


def test_top_recommendation_targets_downtime(tmp_path, line_def):
    recs = _recs(tmp_path, line_def)
    assert recs[0]["category"] == "DOWNTIME"


def test_recommendations_sorted_by_tl_desc(tmp_path, line_def):
    recs = _recs(tmp_path, line_def)
    tls = [r["tl"] for r in recs]
    assert tls == sorted(tls, reverse=True)


def test_at_least_three_recommendations(tmp_path, line_def):
    recs = _recs(tmp_path, line_def)
    assert len(recs) >= 3
```

- [ ] **Step 2: Çalıştır — geçmeli**

Run: `cd backend && pytest tests/test_recommend_priority.py -q`
Expected: 3 passed. (DOWNTIME en büyük TL kaybı olduğundan en üstte; cost_tree zaten azalan.)

- [ ] **Step 3: Commit**

```bash
git add backend/tests/test_recommend_priority.py
git commit -m "test(g9): baseline recommendation priority"
```

## Task G9.4: GET /recommendations endpoint

**Files:**
- Create: `backend/app/api/recommend_routes.py`
- Modify: `backend/app/main.py`
- Test: `backend/tests/test_recommend_endpoint.py`

- [ ] **Step 1: Endpoint testini yaz**

```python
from pathlib import Path

from fastapi.testclient import TestClient

from app.main import app

FIX = Path(__file__).resolve().parent / "fixtures" / "baseline"


def test_recommend_endpoint(tmp_path, monkeypatch):
    monkeypatch.setenv("OEE_DUCKDB_PATH", str(tmp_path / "reco.duckdb"))
    with TestClient(app) as client:
        client.post("/ingest", json={"path": str(FIX)})
        r = client.get("/recommendations")
        assert r.status_code == 200
        recs = r.json()["recommendations"]
        assert len(recs) >= 3
        assert recs[0]["category"] == "DOWNTIME"
        tls = [x["tl"] for x in recs]
        assert tls == sorted(tls, reverse=True)
        for x in recs:
            assert set(x) >= {"category", "tl", "estimated_gain_tl", "title", "action", "assumption"}
```

- [ ] **Step 2: Çalıştır — başarısız olmalı (404)**

Run: `cd backend && pytest tests/test_recommend_endpoint.py -q`
Expected: FAIL (route yok).

- [ ] **Step 3: recommend_routes.py yaz**

```python
"""GET /recommendations?from=&to= -> TL'ye göre sıralı kural tabanlı öneri listesi.

G11 TL ağacı üstüne kurulur; tahmini kazanç modüler GainEstimator arkasında.
ground_truth ALMAZ (firewall).
"""
from __future__ import annotations

from fastapi import APIRouter, Query, Request

from app.analytics.cost import to_tl
from app.analytics.loss_tree import extract_loss_tree
from app.analytics.recommend import RatioGainEstimator, generate_recommendations
from app.config import (
    load_cost_config,
    load_line_definition,
    load_recommend_config,
)

router = APIRouter()


@router.get("/recommendations")
def get_recommendations(
    request: Request,
    frm: str | None = Query(None, alias="from"),
    to: str | None = Query(None),
) -> dict:
    repo = request.app.state.repo
    cfg = request.app.state.config
    line = load_line_definition(cfg.line_config_path)
    costs = load_cost_config(cfg.cost_config_path)
    reco_cfg = load_recommend_config(cfg.recommend_config_path)
    events = repo.fetch_events(frm, to)
    production = repo.fetch_production()
    tree = extract_loss_tree(events, production, line)
    cost = to_tl(tree, costs)
    recs = generate_recommendations(cost, events, reco_cfg, RatioGainEstimator(reco_cfg))
    return {"recommendations": recs}
```

- [ ] **Step 4: main.py'de router kaydet**

Import bloğuna:

```python
from app.api.recommend_routes import router as recommend_router
```

`include_router` bloğuna (`app.include_router(cost_router)` ardına):

```python
app.include_router(recommend_router)
```

- [ ] **Step 5: Çalıştır — geçmeli**

Run: `cd backend && pytest tests/test_recommend_endpoint.py -q`
Expected: passed.

- [ ] **Step 6: Commit**

```bash
git add backend/app/api/recommend_routes.py backend/app/main.py backend/tests/test_recommend_endpoint.py
git commit -m "feat(g9): GET /recommendations endpoint"
```

## Task G9.5: Pano — "Öneriler" bölümü

**Files:**
- Modify: `backend/app/templates/dashboard.html`
- Modify: `backend/app/static/dashboard.js`

- [ ] **Step 1: dashboard.html'e Öneriler bölümü ekle**

TL Pareto kartının ardına ekle (kardeş kartların gerçek sınıf adlarını kullan):

```html
<section class="card" id="recommendations-card">
  <header class="card-head">
    <h3>Öneriler</h3>
    <span class="muted">kural tabanlı · tahmini kazanç varsayımlıdır</span>
  </header>
  <ol id="recommendations" class="reco-list"></ol>
</section>
```

- [ ] **Step 2: dashboard.js'e render ekle**

`renderCostPareto` ardına:

```javascript
function renderRecommendations(data) {
  const list = document.getElementById("recommendations");
  list.innerHTML = "";
  data.recommendations.forEach((r) => {
    const gain = Math.round(r.estimated_gain_tl).toLocaleString("tr-TR");
    const tl = Math.round(r.tl).toLocaleString("tr-TR");
    const li = document.createElement("li");
    li.className = "reco-item";
    li.innerHTML =
      "<div class='reco-head'><strong>" + r.title + "</strong>" +
      "<span class='muted'>" + r.category + " · " + tl + " TL kayıp</span></div>" +
      "<div class='reco-action'>" + r.action + "</div>" +
      "<div class='reco-gain'>Tahmini kazanç: <strong>~" + gain + " TL</strong></div>" +
      "<div class='reco-assumption muted'>" + r.assumption + "</div>";
    list.appendChild(li);
  });
}
```

`load()` içindeki `Promise.all`'a `/recommendations` ekle ve render et:

```javascript
  const [tree, cost, reco, trend, dq] = await Promise.all([
    getJSON("/loss-tree" + q),
    getJSON("/loss-tree/cost" + q),
    getJSON("/recommendations" + q),
    getJSON("/oee/trend?bucket=day" + (q ? "&" + q.slice(1) : "")),
    getJSON("/data-quality/summary"),
  ]);
  renderKpis(oee, dq);
  renderWaterfall(oee);
  renderLossTree(tree);
  renderCostPareto(cost);
  renderRecommendations(reco);
  renderTrend(trend);
```

- [ ] **Step 3: (Opsiyonel) dashboard.css'e basit stil ekle**

`backend/app/static/dashboard.css` sonuna:

```css
.reco-list { list-style: none; margin: 0; padding: 0; display: grid; gap: 12px; }
.reco-item { padding: 12px 14px; border: 1px solid rgba(255,255,255,0.06); border-radius: 10px; }
.reco-head { display: flex; justify-content: space-between; gap: 12px; margin-bottom: 6px; }
.reco-action { font-size: 0.92rem; }
.reco-gain { margin-top: 6px; color: #34d399; }
.reco-assumption { margin-top: 4px; font-size: 0.82rem; }
```

- [ ] **Step 4: Panoyu gözle doğrula**

Run (arka planda):
```bash
cd backend && OEE_DUCKDB_PATH=./demo_g9.duckdb SAMPLE_DATA_DIR=./tests/fixtures/baseline uvicorn app.main:app --port 8019
```
`http://localhost:8019/` → "Öneriler" bölümü; en üstte DOWNTIME hedefli öneri, `ariza_pompa`/istasyon detayı metinde, tahmini kazanç + varsayım satırı görünür. Durdur.

- [ ] **Step 5: Commit**

```bash
git add backend/app/templates/dashboard.html backend/app/static/dashboard.js backend/app/static/dashboard.css
git commit -m "feat(g9): dashboard recommendations section"
```

## Task G9.6: STATUS.md + tam paket + push

**Files:**
- Modify: `docs/STATUS.md`

- [ ] **Step 1: STATUS.md "Mevcut API" / özellik listesine ekle**

Mevcut API listesine `/loss-tree/cost` ve `/recommendations` ekle; ayrıca kısa bir "TL lensi (G11) + Öneriler (G9)" notu yaz (mevcut bölüm üslubuna uygun, 2-3 cümle).

- [ ] **Step 2: Tüm kapıyı çalıştır**

Run: `cd backend && ruff check . && pytest -q`
Expected: ruff temiz; tüm testler yeşil (49 + G6:4 + G11:11 + G9:10 ≈ 74 passed).

- [ ] **Step 3: Regresyon marker'ı hâlâ yeşil**

Run: `cd backend && pytest -m regression -q`
Expected: G6 sözleşme testleri PASS (parite bozulmadı).

- [ ] **Step 4: Commit + push + CI**

```bash
git add docs/STATUS.md
git commit -m "docs(g9): document cost lens + recommendations in STATUS"
git push
gh run watch --exit-status
```
Expected: CI yeşil.

**G9 başarı kriteri:** baseline'da ≥3 öneri, en üst DOWNTIME hedefli, liste TL sıralı; her öneride kategori+TL+tahmini kazanç+varsayım; GainEstimator modüler; tüm testler + G6 kapısı yeşil.

---

## Self-Review (plan ↔ spec kontrolü)

- **G6 kapsam:** ci.yml ✓ (Task G6.4), regresyon sözleşme testi + marker ✓ (G6.1/G6.2), ruff config ✓ (G6.1), Makefile ✓ (G6.3), STATUS notu ✓ (G6.5). Eşik sabitleri (PARITY/LOSSLESS/INFERRED) açık ✓.
- **G11 kapsam:** costs.yaml + CostConfig ✓ (G11.1), cost.py to_tl ✓ (G11.2), /loss-tree/cost ✓ (G11.4), pano TL Pareto + "varsayım" etiketi ✓ (G11.5), 3 test dosyası (mapping/parity/endpoint) ✓ (G11.2/3/4). Parite nüansı (zaman ±%2, kalite geniş, toplam makul) kod yorumu + test sabitlerinde ✓.
- **G9 kapsam:** recommend.yaml ✓ (G9.1), recommend.py + GainEstimator Protocol + RatioGainEstimator ✓ (G9.2), reason_code/istasyon dağılımı `_top_reason_detail` ✓ (G9.2/3), /recommendations ✓ (G9.4), pano Öneriler ✓ (G9.5), 3 test dosyası (priority/content/endpoint) ✓. Garanti dili yasağı testle korunuyor ✓.
- **Tip tutarlılığı:** `to_tl` çıktısı `{categories:[{category,axis,value,tl,kind}], total_tl}` — cost endpoint, parity testi ve recommend.py aynı şekli kullanıyor ✓. `generate_recommendations(cost_tree, events, config, estimator)` imzası içerik/priority testleri ve endpoint'te tutarlı ✓. `RecommendConfig.rules: dict[str, CategoryRule]` her yerde aynı ✓.
- **Firewall:** hiçbir üretim modülü (cost.py, recommend.py, route'lar) `ground_truth` parametresi almıyor; gerçek yalnız conftest `baseline_truth_value` / `baseline_truth_cost` ile testte ✓.
- **Açık varsayım (yürütücü için):** Pano HTML/CSS sınıf adları dosyadan birebir okunmalı (grep adımları eklendi); `compute_oee` import yolu G6.2 Step 2'de gerçek imzayla doğrulanmalı.
