services:
  oee-platform:
    build: ./backend
    ports:
      - "8000:8000"
    environment:
      - OEE_DUCKDB_PATH=/data/oee.duckdb
      - OEE_LINE_CONFIG=/app/config/line_default.yaml
    volumes:
      - ./config:/app/config:ro
      - oee-data:/data
volumes:
  oee-data:
